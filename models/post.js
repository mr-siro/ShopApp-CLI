class Post {
    constructor(id, ownerId, title, img, description) {
        this.id = id;
        this.ownerId = ownerId;
        this.img = img;
        this.title = title;
        this.description = description;
    }
}

export default Post;
